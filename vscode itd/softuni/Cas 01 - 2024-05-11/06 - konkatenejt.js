function concatenatedata(firstname, lastname, age, town) {
    let result = `You are ${firstname} ${lastname}, a ${age} years old person from ${town}`;
    console.log(result);
}

concatenatedata("Luka", "Vukosavljevic", 16, "Novo Selo")
